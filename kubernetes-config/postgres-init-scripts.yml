kubectl create configmap postgres-init-scripts --from-literal=init-db.sql='
DO
$$
BEGIN
   IF NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = ''appdb'') THEN
      EXECUTE ''CREATE DATABASE appdb'';
   END IF;
END
$$;
'
